# タスク: 最新 YouTube DOM 調査・代替手法探索

## 実施したタスク

### 完了
- 動画ページ関連動画・検索結果・チャンネルグリッドの動画要素を調査
- 判定シグナル（badge-shape のクラス、コレクションサムネ、認証アーティスト、内部データ）を洗い出し
- CSS `:has()` のみでのブロック/許可をプロトタイプ検証
- `docs/workflow/research/youtube-dom-2026-09.md` にまとめ

## 技術的決定事項

### TD-1: 推奨手法
- **決定**: 判定を CSS `:has()` に移す方向を推奨（実装は未着手）
- **代替案**: JS バッジ判定の継続 / MAIN world で内部データ読取 / ネットワーク応答書換え
- **理由**: 遅延読込にも即時適用、JS 不要、セレクタ修正箇所が1か所。内部データはページごとに構造が違い脆い

## 学び
- `yt-lockup-view-model` が主要ページの共通動画要素になった
- 「音楽」はロックアップの DOM からは判定不能（認証アーティスト表示はダイアログ内に隠れている）
- content script は isolated world なので要素の JS プロパティ（`rawProps`）は読めない

## 次のアクション
- 許可する対象（ライブ・ミックスリストのみでよいか）を決める
- `:has()` 移行の設計（business/spec/archi wip）
- 未確認: ホーム・Shorts プレイヤー（要ログイン）、全画面グリッド
